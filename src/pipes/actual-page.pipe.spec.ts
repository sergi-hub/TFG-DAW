import { ActualPagePipe } from './actual-page.pipe';

describe('ActualPagePipe', () => {
  it('create an instance', () => {
    const pipe = new ActualPagePipe();
    expect(pipe).toBeTruthy();
  });
});
